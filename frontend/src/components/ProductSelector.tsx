// frontend/src/components/ProductSelector.tsx (stub — will be replaced in Task 10)
interface Props { sensorId: string; sensorName: string; }
export default function ProductSelector(_: Props) { return null; }
